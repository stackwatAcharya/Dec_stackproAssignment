import React, { useState } from 'react'

interface foodCategoriesProps {
    data: any,
    handleClick?: () => void
}

const LocationCards: React.FC<foodCategoriesProps> = ({
    data, handleClick
}) => {

    return (
        <div>

        </div>
    )
}

export default LocationCards